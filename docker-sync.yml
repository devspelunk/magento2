version: "2"
options:
  verbose: true
syncs:
  moc-magento-sync:
    sync_userid: '1000'
    host_disk_mount_mode: 'cached'
    src: './src'
    # watch_args: '-v'
    sync_excludes: [
    '.idea',
    '.git',
    'var/cache',
    'var/sessions',
    'var/page_cache',
    'var/generation',
    'var/composer_home',
    'var/view_preprocessed',
    'var/session',
    'var/tmp',
    'node_modules',
    #'vendor/*',
    #'pub/static'
    ]